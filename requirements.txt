# --- Data pipeline (Phases 1-4) ---
# Pure-Python, works on CPU/Windows.
pytest>=8.0

# --- Tokenizer (Phase 5) ---
tokenizers>=0.20

# --- Modeling (Phases 6-7) ---
# Install torch separately with the right CUDA build for your GPU box:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# transformers is pinned <5 and peft to 0.17.x: peft 0.18-0.19.1 imports
# EmbeddingParallel from transformers.integrations.tensor_parallel during
# checkpoint resume (set_peft_model_state_dict), which fails on both
# transformers 5.0 and late-4.x — 0.17.1 predates that code path entirely.
transformers>=4.46,<5
peft==0.17.*
accelerate>=1.0
datasets>=3.0
trl>=0.11
# bitsandbytes is Linux/CUDA-first; install on the training box, not local Windows dev:
#   bitsandbytes>=0.44

# --- Logging (optional) ---
wandb>=0.18

# --- Demo (optional, Phase 8) ---
gradio>=5.0
